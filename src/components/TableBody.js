// make table body compontnts
// import tbdoy compo. into table .js
// pass the user from the state to table.js and then tablebody
// map over every user in the array and create a new Table for every user
// styling
// sorting functionality, method .sort

import React, { Component } from 'react'

class TableBody extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}

export default TableBody;
